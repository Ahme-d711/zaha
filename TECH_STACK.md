# تفاصيل التقنيات المستخدمة في المشروع (Tech Stack)

تم بناء هذا المشروع باستخدام أحدث التقنيات لضمان الأداء العالي، القابلية للتوسع، وتجربة مستخدم ممتازة.

## 🌐 اللغات البرمجية (Languages)
*   **TypeScript**: اللغة الأساسية المستخدمة في كل من الفرونت-إند والباك-إند لضمان كتابة كود آمن وسهل الصيانة.
*   **JavaScript**: تستخدم في ملفات الإعدادات والتهيئة.

---

## 💻 الواجهة الأمامية (Frontend)
تم بناء الواجهة الأمامية باستخدام إطار عمل **React** مع أداة البناء **Vite**.

### المكتبات الأساسية:
*   **React 18**: المكتبة الأساسية لبناء واجهات المستخدم.
*   **Tailwind CSS**: لتنسيق الواجهات بشكل عصري وسريع.
*   **Shadcn UI & Radix UI**: لتوفير مكونات واجهة مستخدم جاهزة، احترافية، وقابلة للوصول (Accessible).
*   **TanStack Query (React Query)**: لإدارة جلب البيانات من الباك-إند وتخزينها مؤقتاً (Caching).
*   **Zustand**: لإدارة الحالة العالمية للمشروع (Global State Management) بشكل خفيف وبسيط.
*   **Framer Motion**: لإضافة حركات وأنيميشن تفاعلية وسلسة.
*   **React Router DOM**: لإدارة التنقل بين الصفحات (Routing).
*   **Axios**: لإرسال الطلبات إلى الـ API.
*   **Zod**: للتحقق من صحة البيانات (Validation) في النماذج.
*   **Lucide React**: حزمة الأيقونات المستخدمة في المشروع.

---

## ⚙️ الواجهة الخلفية (Backend)
تم بناء الباك-إند باستخدام بيئة تشغيل **Node.js** مع محرك **Bun** للسرعة الفائقة.

### التقنيات الأساسية:
*   **Express.js**: إطار العمل الأساسي لبناء الـ API.
*   **MongoDB & Mongoose**: قاعدة البيانات (NoSQL) ومكتبة الربط معها.
*   **Bun**: محرك تشغيل الكود السريع جداً وبديل npm/node في التطوير.
*   **JSON Web Token (JWT)**: لتأمين عملية تسجيل الدخول والتحقق من هوية المستخدمين.
*   **BcryptJS**: لتشفير كلمات المرور بشكل آمن.
*   **Multer**: لإدارة رفع الملفات والصور وتخزينها.
*   **Swagger UI**: لتوفير توثيق تفاعلي للـ API (Documentation).
*   **Zod**: للتحقق من صحة البيانات القادمة من المستخدم قبل معالجتها.
*   **Morgan & Helmet**: لزيادة الأمان ومراقبة سجلات الطلبات (Logging).

---

## 🛠️ أدوات التطوير (Dev Tools)
*   **ESLint**: لضمان جودة الكود واتباع أفضل الممارسات.
*   **PostCSS & Autoprefixer**: لمعالجة ملفات CSS وضمان توافقها مع المتصفحات.
*   **Nodemon**: لإعادة تشغيل الخادم تلقائياً عند تغيير الكود (في حالة عدم استخدام Bun).
